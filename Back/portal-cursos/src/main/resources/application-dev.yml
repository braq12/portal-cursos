server:
  port: 8085

spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: wowbraq@gmail.com
    password: jlfo rskt zanp qfoc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  servlet:
    multipart:
      max-file-size: 512MB
      max-request-size: 512MB
  datasource:
    url: jdbc:mysql://skillup-database.c34y04g262ik.us-east-2.rds.amazonaws.com:3306/portal_cursos?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: admin
    password: admin.123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

jwt:
  secret: DEV_LLAVE_LARGA_LLAVE_LARGA_12345678901234567890
  expirationMinutes: 120

management:
  endpoints:
    web:
      exposure:
        include: health,info

storage:
  provider: s3

aws:
  region: us-east-2
  s3:
    bucket: portal-cursos-bucket
    public-base-url: https://portal-cursos-bucket.s3.us-east-2.amazonaws.com

CORS_ALLOWED_ORIGINS: "http://localhost:4200"
